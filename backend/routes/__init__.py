# backend/routes/__init__.py
# Marks routes/ as a Python package so we can do `from routes.health import router`.
# An empty file is enough — its presence tells Python "this dir is a module package".
